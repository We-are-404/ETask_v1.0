/* * * * * * * * * * * * * * * * * * *
 *
 *				DOM
 *
 * * * * * * * * * * * * * * * * * * */
const inputCheck = () => {
	const input = $('.left_header_search_input');
	let timer = null;
	input.keyup(() => {
		// if(timer!==null){
		// 	clearTimeout(timer)
		// }
		// timer = setTimeout(()=>{
			
		// },500)
	})
}
inputCheck()
